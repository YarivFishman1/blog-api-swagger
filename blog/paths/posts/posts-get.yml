tags:
  - Posts
summary: Return a post
description: Return a post with postId equals to id.
parameters:
  - name: id
    in: path
    required: true
    description: The postId requested by user.
    schema:
      type: integer
responses:
  "200":
    description: A blog posts in JSON format
    content:
      application/json:
        schema:
          $ref: "../../definition/post.yml"
