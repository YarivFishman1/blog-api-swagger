tags:
  - Posts
summary: Update a post
description: Update the post with id partially.
parameters:
  - name: id
    in: path
    required: true
    description: The postId requested by user.
    schema:
      type: string
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "../../definition/patch_post.yml"
responses:
  "200":
    description: Updated the post partially.
